# Building
## Downloading deps
### Ubuntu
`sudo apt install gcc make libreadline-dev`
### Fedora
`sudo dnf install gcc make readline-devel`

## Compiling
For debug build:
`make debug`
For release build:
`make release`

